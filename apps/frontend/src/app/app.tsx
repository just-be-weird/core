import styled from '@emotion/styled';
import {Theme} from '@core/theme'

const StyledApp = styled.div`
  // Your style here
`;

export function App() {
  return (
    <StyledApp>
<Theme/>
    </StyledApp>
  );
}

export default App;
